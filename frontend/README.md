# Frontend

Los comandos necesarios para que el frontend funcione el frontend, son los siguientes:

Instalar las dependencias: npm install
Iniciar el servidor de desarrollo: ng serve

Una vez ejecutado ng serve, abre tu navegador y ve a http://localhost:4200/. La aplicación se recargará automáticamente al modificar archivos fuente.
